services:
  urlfinder-mcp:
    image: hackerdogs/urlfinder-mcp:latest
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8112:8112"
    environment:
      - MCP_TRANSPORT=streamable-http
      - MCP_PORT=8112
    restart: unless-stopped
