version: "3.8"

services:
  maigret-mcp:
    image: hackerdogs/maigret-mcp:latest
    build:
      context: .
      dockerfile: Dockerfile
    container_name: maigret-mcp
    ports:
      - "8221:8221"
    environment:
      - MCP_TRANSPORT=streamable-http
      - MCP_PORT=8221
    restart: unless-stopped
