version: "3.8"

services:
  upx-mcp:
    image: hackerdogs/upx-mcp:latest
    build:
      context: .
      dockerfile: Dockerfile
    container_name: upx-mcp
    ports:
      - "8284:8284"
    environment:
      - MCP_TRANSPORT=streamable-http
      - MCP_PORT=8284
    restart: unless-stopped
