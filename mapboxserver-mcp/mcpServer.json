{
  "mcpServers": {
    "mapboxserver-mcp": {
      "command": "docker",
      "args": ["run", "-i", "--rm", "-e", "MCP_TRANSPORT", "-e", "MCP_PORT", "hackerdogs/mapboxserver-mcp:latest"],
      "env": {
        "MCP_TRANSPORT": "stdio",
        "MCP_PORT": "8429"
      }
    }
  }
}
