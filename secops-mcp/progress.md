# SecOps MCP — Progress
- [x] FastMCP server with `list_tools` and `run_secops_tool` (whitelisted CLIs)
- [x] Dockerfile (Python-only; add tools via extend or PATH); stdio + streamable-http, test.sh, README, publish script
