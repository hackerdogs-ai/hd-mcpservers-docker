{
  "mcpServers": {
    "zscaler-mcp-server-mcp": {
      "command": "docker",
      "args": ["run", "-i", "--rm", "-e", "MCP_TRANSPORT", "-e", "MCP_PORT", "hackerdogs/zscaler-mcp-server-mcp:latest"],
      "env": {
        "MCP_TRANSPORT": "stdio",
        "MCP_PORT": "8463"
      }
    }
  }
}
