services:
  asnmap-mcp:
    image: hackerdogs/asnmap-mcp:latest
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8110:8110"
    environment:
      - MCP_TRANSPORT=streamable-http
      - MCP_PORT=8110
      - PDCP_API_KEY=${PDCP_API_KEY}
    restart: unless-stopped
