version: "3.8"

services:
  holehe-mcp:
    image: hackerdogs/holehe-mcp:latest
    build:
      context: .
      dockerfile: Dockerfile
    container_name: holehe-mcp
    ports:
      - "8219:8219"
    environment:
      - MCP_TRANSPORT=streamable-http
      - MCP_PORT=8219
    restart: unless-stopped
