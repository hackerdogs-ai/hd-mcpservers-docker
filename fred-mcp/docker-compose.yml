version: "3.8"

services:
  fred-mcp:
    image: hackerdogs/fred-mcp:latest
    build:
      context: .
      dockerfile: Dockerfile
    container_name: fred-mcp
    ports:
      - "8423:8423"
    environment:
      - MCP_TRANSPORT=streamable-http
      - MCP_PORT=8423
    restart: unless-stopped
