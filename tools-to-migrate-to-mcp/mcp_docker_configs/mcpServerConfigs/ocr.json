{
  "mcpServers": {
    "ocr": {
      "command": "docker",
      "args": [
        "run",
        "--rm",
        "-i",
        "--env",
        "PYTHONUNBUFFERED=1",
        "hackerdogs/hackerdogs-mcp-ocr:latest"
      ],
      "description": "MCP OCR Server - Extract text from images using Tesseract OCR. Supports local files, URLs, and image bytes. Published Docker image from hackerdogs Docker Hub."
    }
  }
}


