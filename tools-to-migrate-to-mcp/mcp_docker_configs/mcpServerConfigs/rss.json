{
  "mcpServers": {
    "rss": {
      "command": "docker",
      "args": [
        "run",
        "--rm",
        "-i",
        "--env",
        "NODE_ENV=production",
        "--env",
        "NODE_OPTIONS=--no-warnings",
        "hackerdogs/rss-mcp:latest"
      ],
      "description": "MCP RSS Server - Fetch and parse RSS feeds using rss-mcp npm package. Fixed to work around missing shebang in package binary. Published Docker image from hackerdogs Docker Hub."
    }
  }
}


