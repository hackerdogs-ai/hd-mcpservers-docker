version: "3.8"
services:
  sentry-mcp:
    image: hackerdogs/sentry-mcp:latest
    build: .
    ports:
      - "8665:8665"
    environment:
      - MCP_TRANSPORT=streamable-http
      - MCP_PORT=8665
      - SENTRY_AUTH_TOKEN=${SENTRY_AUTH_TOKEN:-}
