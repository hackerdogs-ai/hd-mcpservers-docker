version: "3.8"
services:
  gitlab-mcp:
    image: hackerdogs/gitlab-mcp:latest
    build: .
    ports:
      - "8642:8642"
    environment:
      - MCP_TRANSPORT=streamable-http
      - MCP_PORT=8642
      - GITLAB_TOKEN=${GITLAB_TOKEN:-}
