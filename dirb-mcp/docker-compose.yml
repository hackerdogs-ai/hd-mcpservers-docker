version: "3.8"

services:
  dirb-mcp:
    image: hackerdogs/dirb-mcp:latest
    build:
      context: .
      dockerfile: Dockerfile
    container_name: dirb-mcp
    ports:
      - "8214:8214"
    environment:
      - MCP_TRANSPORT=streamable-http
      - MCP_PORT=8214
    restart: unless-stopped
