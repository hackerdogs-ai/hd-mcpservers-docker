version: "3.8"

services:
  gef-mcp:
    image: hackerdogs/gef-mcp:latest
    build:
      context: .
      dockerfile: Dockerfile
    container_name: gef-mcp
    ports:
      - "8238:8238"
    environment:
      - MCP_TRANSPORT=streamable-http
      - MCP_PORT=8238
    restart: unless-stopped
