{
  "mcpServers": {
    "ctgov-mcp-docker-mcp": {
      "command": "docker",
      "args": ["run", "-i", "--rm", "-e", "MCP_TRANSPORT", "-e", "MCP_PORT", "hackerdogs/ctgov-mcp-docker-mcp:latest"],
      "env": {
        "MCP_TRANSPORT": "stdio",
        "MCP_PORT": "8401"
      }
    }
  }
}
