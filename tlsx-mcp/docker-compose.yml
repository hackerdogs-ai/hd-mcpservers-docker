services:
  tlsx-mcp:
    image: hackerdogs/tlsx-mcp:latest
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8109:8109"
    environment:
      - MCP_TRANSPORT=streamable-http
      - MCP_PORT=8109
    restart: unless-stopped
